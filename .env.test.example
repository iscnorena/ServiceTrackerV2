# Base de datos EXCLUSIVA para pruebas: la suite hace TRUNCATE de todas las
# tablas antes de cada archivo. El nombre debe contener "_test" o la suite se
# niega a correr (ver tests/setup.ts).
DATABASE_URL="postgresql://st2:st2pass@localhost:55432/servicetracker_test?schema=public"
AUTH_SECRET="test-secret-solo-para-pruebas-0123456789"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
CRON_SECRET="test-cron-secret"
